// This file is deprecated. Environment variables are now accessed directly via `process.env`
// where needed. This change was made to resolve startup errors (blank screen) caused by
// `import.meta.env` being unavailable in the execution environment.
